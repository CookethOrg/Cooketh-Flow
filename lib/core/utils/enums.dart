enum ProviderState { inital, empty, loading, loaded, success, error }
enum NodeType {note, rectangular,diamond,database,parallelogram}
enum ConnectionPoint {up,down,left,right}
enum ConnectionType {solid,dotted,dashed}