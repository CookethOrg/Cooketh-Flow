// import 'dart:math';
// import 'dart:ui';

import 'package:cookethflow/core/utils/state_handler.dart';

class FlowProvider extends StateHandler {
  FlowProvider() : super();

  // final List<Offset> _nodePositions = [Offset(100, 100), Offset(200, 300)];
  // final List<List<int>> connections = []; // Stores connections between nodes
  // final double scale = 1.0; // Initial zoom scale
  // final double lastScale = 1.0; // Last scale factor for pinch-to-zoom
  // List<Offset> get nodePositions => _nodePositions;

  // void addNewNode() {
  //   _nodePositions.add(Offset(
  //     Random().nextDouble() * 300, // Random X position
  //     Random().nextDouble() * 600, // Random Y position
  //   ));
  //   notifyListeners();
  // }
}
